[pytest]
testpaths = tests
pythonpath = . tests
addopts = -ra --strict-markers
markers =
    integration: requires live nflverse network access
filterwarnings =
    ignore::DeprecationWarning:google.*
